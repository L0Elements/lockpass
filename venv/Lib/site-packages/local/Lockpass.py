'''
Python 3.10.10
By L0Elements
'''
import os
import datetime

def error():
#    print("An error has occurred, trying to autofix")
    lockpass.initialize()
#    print("Autofix Done! Try to repeat the command, if the problem persists, contact the support")
    return 1
class lockpass():

    def newbook(name, description, passwd):
        try:
            os.mkdir("Data/books/" + name)
        except FileNotFoundError:
            error()
            return 1
        with open("Data/books/" + name +"/.bookinfo", "w") as f:
            f.write("Name: " + name + '\n' + "Created on: " + str(datetime.date.today()) + "\nDescription: " + description)
        return 0
    def help():
        pass
    def initialize():
        try:
            os.mkdir("Data")
        except:
            pass
        try:
            os.mkdir("Data/books")
        except:
            pass
